- id: gh-actions-updater
  name: gh-actions-updater
  description: Check GitHub Actions references for available latest-tag updates.
  entry: gh-actions-updater --check
  language: rust
  pass_filenames: false
  stages: [pre-commit]
