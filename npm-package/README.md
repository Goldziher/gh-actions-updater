# gh-actions-updater

npm wrapper for the `gh-actions-updater` Rust CLI.

```bash
npm install -g gh-actions-updater
gh-actions-updater --help
```

The postinstall script downloads the matching binary from GitHub Releases. No
platform binaries are checked into the npm package.
